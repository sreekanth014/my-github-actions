name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Here is the message in GitHub Actions"

      - name: Run tests
        run:  npm run test -- src/utils/plusTwo.test.ts